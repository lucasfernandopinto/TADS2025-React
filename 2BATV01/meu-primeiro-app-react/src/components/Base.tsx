// importa o React da biblioteca 'react'.
import React from 'react';

// cria um componente chamado 'Base'.
function Base() {
  // retorna o que será renderizado na tela.
  return (
    // o componente retorna um footer com o parágrafo "Rodapé da página"
    <footer>
      <p>Rodapé da página</p>
    </footer>
  );
}

// exporta o componente 'Base' para ser usado em outros arquivos.
export default Base;
