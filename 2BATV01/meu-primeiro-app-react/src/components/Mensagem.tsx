// importa o React da biblioteca 'react'.
import React from 'react';

// cria um componente chamado 'Mensagem'.
function Mensagem() {
  // contém o que será renderizado na tela.
  return ( 
    // o componente retorna um paragrafo com o texto "Este é um componente reutilizável."
    <p>Este é um componente reutilizável.</p>
  );
}

// exporta o componente 'Mensagem' como o export padrão deste módulo.
export default Mensagem;
