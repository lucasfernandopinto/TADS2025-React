// importa o React da biblioteca 'react'.
import React from 'react';

// cria um componente chamado 'Meio'.
function Meio() {
  // retorna o que será renderizado na tela.
  return (
    // o componente retorna um main com o parágrafo "Conteúdo do meio da página"
    <main>
      <p>Conteúdo do meio da página</p>
    </main>
  );
}

// exporta o componente 'Meio' para ser usado em outros arquivos.
export default Meio;
