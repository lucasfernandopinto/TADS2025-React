// importa o React da biblioteca 'react'.
import React from 'react';

// cria um componente chamado 'Conteudo'.
function Conteudo() {
  // retorna o que será renderizado na tela.
  return (
    // o componente retorna um main com o parágrafo "Este é o conteúdo principal."
    <main>
      <p>Este é o conteúdo principal.</p>
    </main>
  );
}

// exporta o componente 'Conteudo' para ser usado em outros arquivos.
export default Conteudo;
